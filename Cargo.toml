[package]
name = "history-to-md"
version = "0.1.0"
edition = "2024"
description = "Generate markdown context from git history"

[dependencies]
globset = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
