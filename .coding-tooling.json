{
  "schemaVersion": 1,
  "profile": "repository-foundation-v1",
  "tiers": {
    "fast": ["lint", "build", "test:unit"]
  },
  "requiredCapabilities": ["lint", "build", "test:unit"],
  "optionalCapabilities": ["format:check"]
}
